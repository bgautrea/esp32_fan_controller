#pragma once
// ---------------------------------------------------------------------
//  Template. Copy this file to secrets.h and fill in your WiFi details.
//  secrets.h is gitignored, so your credentials never get committed.
// ---------------------------------------------------------------------

// WiFi credentials
#define WIFI_SSID      "YOUR_WIFI_SSID"
#define WIFI_PASSWORD  "YOUR_WIFI_PASSWORD"

// OTA password. Used as the HTTP basic-auth password (username "admin")
// when pushing firmware to /update from the web UI.
#define OTA_PASSWORD   "fancontrol123"
